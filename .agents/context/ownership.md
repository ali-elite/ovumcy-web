# Ownership

- `ovumcy-web` owns the self-hosted all-in-one web product shape and server-delivered browser/PWA behavior.
- It is not automatically authoritative for app-domain behavior, managed-cloud policy, or self-hosted app-sync policy.
- Cross-repo authority should be resolved through `D:\ovumcy-governance`.
